interface:
  display_name: "Code Janitor"
  short_description: "Prove and remove accidental codebase complexity"
  default_prompt: "Use $code-janitor to audit this repository and rank the safest high-impact simplifications without editing files."

policy:
  allow_implicit_invocation: true

